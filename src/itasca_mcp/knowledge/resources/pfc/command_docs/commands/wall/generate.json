{
  "category": "wall",
  "search_keywords": [
    "generate",
    "box",
    "cylinder"
  ],
  "description": "Generate walls with specified shapes. The wall shapes include: {box, circle, and point in 2D; box, cone, cylinder, disk, line, plane, point, polygon, and sphere in 3D}. Note: A model domain must be specified prior to wall creation. All wall facets must fall within the model domain. The id, name, and group keywords must be given prior to any additional keywords. While cycling, walls can only be created before cycle point 0 (i.e., when the timestep is calculated).",
  "python_sdk_alternative": {
    "available": false,
    "workaround": "itasca.command('wall generate ...') - Python SDK does not provide wall geometry generation"
  },
  "notes": [
    "IMPORTANT: The id, name, and group keywords must be given prior to any additional keywords (shape keywords like box, cylinder, etc.)",
    "This command is designed for generating complete geometric bodies composed of multiple wall elements (e.g., box creates 6 separate walls by default)",
    "For box shape: Each face gets auto-generated name suffix (Top/Bottom/Left/Right/Front/Back + ID)",
    "Python SDK access requires finding each face individually: itasca.wall.find('nameTopXX') where XX is wall ID",
    "For independent control of individual faces with custom names, use 'wall create' instead to avoid name concatenation"
  ],
  "versions": {
    "7.0": {
      "command": "wall generate",
      "syntax": "wall generate keyword ...",
      "keywords": [
        {
          "name": "group",
          "syntax": "group <s>[slot <slot>] [facet] ...",
          "description": "Assign name s to the group name at slot slot for the created wall or its constituent facets. If the slot keyword is not specified, then the group name is assigned to the slot Default . The optional facet keyword specifies that the group is assigned to the facets and not to the wall."
        },
        {
          "name": "id",
          "syntax": "id <id>",
          "description": "Wall ID. If not specified, then the next available ID is used."
        },
        {
          "name": "name",
          "syntax": "name <s>",
          "description": "Wall name. If not specified, then each shape defines a wall name with the ID post pended."
        },
        {
          "name": "box",
          "syntax": "box <fxmin><fxmax><fymin><fymax><fzmin><fzmax>[one-wall] [expand <fe>]",
          "description": "Generate a closed box. If the name keyword has not been specified, then s = boxWall . By default, each side of the box is a separate wall. In this case, the wall name is concatenated with the wall side name (e.g., {top, bottom, left, or right in 2d; top, bottom, left, right, front, or back in 3D}) and the wall ID. For instance, if s = fred , the name of the top wall is fredTopXX , where XX is the wall ID. A unique ID, starting with id , is assigned to each wall. Use the one-wall keyword to specify that all box sides belong to a single wall. If fxmin and fxmax are specified alone, then the box is a cube with equal sides. Use the optional expand keyword to expand each side of the box by a factor fe ."
        },
        {
          "name": "polygon",
          "syntax": "polygon <vpos>... [make-planar] [triangulate]",
          "description": "Generate a planar, polygonal wall in 3D. If the name keyword has not been specified, then s = polygonWall . There are two modes of operation: the default mode which is guaranteed to work for convex polygons and the triangulation mode, specified with the optional triangulate keyword, that works for both convex and concave polygons. In both modes, the makeplanar keyword is used to snap the vertices to the plane with point equal to the average location of vertices (called the center). The plane normal is defined by the cross product of two vectors, namely the vector difference between the center and vertex 1 and the vector difference between the center and vertex 2. In the default mode, the vertices of the wall can be input in any order and the command is guaranteed to work for sets of vertices defining convex polygons. The algorithm proceeds by ordering the points in clockwise order about the center. The first facet is created from the [1,2,num] vertex where num is the total number of vertices. The next facet is made from the [2,num,3] vertices, and the following from the [num,3,num-1] vertices, and so on. In triangulation mode, the vertices must be input in an ordered fashion around the perimeter of the polygon. The sweep line algorithm is used to triangulate the polygon."
        }
      ],
      "examples": [
        {
          "command": "wall generate box -10 10",
          "description": "Generate cube with sides at ±10 (separate walls for each face)"
        },
        {
          "command": "wall generate box -5 5 -3 3 -2 2 one-wall",
          "description": "Generate rectangular box as single wall (3D)"
        },
        {
          "command": "wall generate sphere position (0,0,5) radius 2.5",
          "description": "Generate sphere at (0,0,5) with radius 2.5 (3D)"
        },
        {
          "command": "wall generate cylinder base (0,0,0) axis (0,0,1) height 10 radius 3 cap false true",
          "description": "Generate cylinder with no bottom cap, top cap only (3D)"
        },
        {
          "command": "wall generate disk radius 5.0 dip 45.0 dip-direction 90.0",
          "description": "Generate disk with 45° dip, 90° dip direction (3D)"
        },
        {
          "command": "wall generate cone base (-2.5,0,-5) axis (1,1,1) height 2.0 radius 0.1 2.5 cap true true resolution 0.5 one-wall",
          "description": "Generate conic frustum as single wall with both caps (3D)"
        },
        {
          "command": "wall generate circle position (0,0) radius 3.0 resolution 0.05",
          "description": "Generate circle at origin with radius 3.0, fine resolution (2D)"
        },
        {
          "command": "wall generate plane position (0,0,10) dip 30 dip-direction 45",
          "description": "Generate plane through (0,0,10) with 30° dip at 45° direction (3D)"
        },
        {
          "command": "wall generate name 'boundary' group 'walls' box -10 10 -10 10 0 20",
          "description": "Generate named box in group 'walls' (3D)"
        }
      ],
      "figure_reference": {
        "figures": [
          {
            "name": "cones.png",
            "doc_path": "_images/cones.png"
          },
          {
            "name": "spheres.png",
            "doc_path": "_images/spheres.png"
          }
        ],
        "note": "This command embeds reference figures that are not captured in this text corpus. Figures are PNGs under <doc-root>/_images/ (doc-root sits beside the engine install, e.g. C:/Program Files/Itasca/<product>/exe64/doc); read them directly."
      },
      "notes": [
        "resolution/position are not live on 7.00; the duplicate keyword entries were crawler artifacts."
      ]
    },
    "6.0": {
      "command": "wall generate",
      "syntax": "wall generate keyword ...",
      "keywords": [
        {
          "name": "box",
          "syntax": "box <fxmin><fxmax><fymin><fymax><fzmin><fzmax>[one-wall]",
          "description": "Generate a closed box. If the name keyword has not been specified, then s = boxWall . By default, each side of the box is a separate wall. In this case, the wall name is concatenated with the wall side name (e.g., {top, bottom, left, or right in 2d; top, bottom, left, right, front, or back in 3D}) and the wall ID. For instance, if s = fred , the name of the top wall is fredTopXX , where XX is the wall ID. A unique ID, starting with id , is assigned to each wall. Use the one-wall keyword to specify that all box sides belong to a single wall. If fxmin and fxmax are specified alone, then the box is a cube with equal sides."
        },
        {
          "name": "cone",
          "syntax": "cone",
          "description": ""
        },
        {
          "name": "cylinder",
          "syntax": "cylinder",
          "description": ""
        },
        {
          "name": "disk",
          "syntax": "disk",
          "description": ""
        },
        {
          "name": "group",
          "syntax": "group <s>[slot <slot>] [facet] ...",
          "description": "Assign name s to the group name at slot slot for the created wall or its constituent facets. If the slot keyword is not specified, then the group name is assigned to the slot Default . The optional facet keyword specifies that the group is assigned to the facets and not to the wall."
        },
        {
          "name": "id",
          "syntax": "id <id>",
          "description": "Wall ID. If not specified, then the next available ID is used."
        },
        {
          "name": "line",
          "syntax": "line",
          "description": ""
        },
        {
          "name": "name",
          "syntax": "name <s>",
          "description": "Wall name. If not specified, then each shape defines a wall name with the ID post pended."
        },
        {
          "name": "plane",
          "syntax": "plane",
          "description": ""
        },
        {
          "name": "point",
          "syntax": "point",
          "description": ""
        },
        {
          "name": "polygon",
          "syntax": "polygon <vpos>... [make-planar] [triangulate]",
          "description": "Generate a planar, polygonal wall in 3D. If the name keyword has not been specified, then s = polygonWall . There are two modes of operation: the default mode which is guaranteed to work for convex polygons and the triangulation mode, specified with the optional triangulate keyword, that works for both convex and concave polygons. In both modes, the makeplanar keyword is used to snap the vertices to the plane with point equal to the average location of vertices (called the center). The plane normal is defined by the cross product of two vectors, namely the vector difference between the center and vertex 1 and the vector difference between the center and vertex 2. In the default mode, the vertices of the wall can be input in any order and the command is guaranteed to work for sets of vertices defining convex polygons. The algorithm proceeds by ordering the points in clockwise order about the center. The first facet is created from the [1,2,num] vertex where num is the total number of vertices. The next facet is made from the [2,num,3] vertices, and the following from the [num,3,num-1] vertices, and so on. In triangulation mode, the vertices must be input in an ordered fashion around the perimeter of the polygon. The sweep line algorithm is used to triangulate the polygon."
        },
        {
          "name": "sphere",
          "syntax": "sphere",
          "description": ""
        }
      ],
      "examples": [
        {
          "command": "wall generate box -10 10",
          "description": "Generate cube with sides at ±10 (separate walls for each face)"
        },
        {
          "command": "wall generate box -5 5 -3 3 -2 2 one-wall",
          "description": "Generate rectangular box as single wall (3D)"
        },
        {
          "command": "wall generate sphere position (0,0,5) radius 2.5",
          "description": "Generate sphere at (0,0,5) with radius 2.5 (3D)"
        },
        {
          "command": "wall generate cylinder base (0,0,0) axis (0,0,1) height 10 radius 3 cap false true",
          "description": "Generate cylinder with no bottom cap, top cap only (3D)"
        },
        {
          "command": "wall generate disk radius 5.0 dip 45.0 dip-direction 90.0",
          "description": "Generate disk with 45° dip, 90° dip direction (3D)"
        },
        {
          "command": "wall generate cone base (-2.5,0,-5) axis (1,1,1) height 2.0 radius 0.1 2.5 cap true true resolution 0.5 one-wall",
          "description": "Generate conic frustum as single wall with both caps (3D)"
        },
        {
          "command": "wall generate circle position (0,0) radius 3.0 resolution 0.05",
          "description": "Generate circle at origin with radius 3.0, fine resolution (2D)"
        },
        {
          "command": "wall generate plane position (0,0,10) dip 30 dip-direction 45",
          "description": "Generate plane through (0,0,10) with 30° dip at 45° direction (3D)"
        },
        {
          "command": "wall generate name 'boundary' group 'walls' box -10 10 -10 10 0 20",
          "description": "Generate named box in group 'walls' (3D)"
        }
      ],
      "figure_reference": {
        "figures": [
          {
            "name": "cones.png",
            "doc_path": "_images/cones.png"
          },
          {
            "name": "spheres.png",
            "doc_path": "_images/spheres.png"
          }
        ],
        "note": "This command embeds reference figures that are not captured in this text corpus. Figures are PNGs under <doc-root>/_images/ (doc-root sits beside the engine install, e.g. C:/Program Files/Itasca/<product>/exe64/doc); read them directly."
      }
    },
    "9.0": {
      "command": "wall generate",
      "syntax": "wall generate keyword ...",
      "keywords": [
        {
          "name": "group",
          "syntax": "group <s>[slot <slot>] [facet] ...",
          "description": "Assign name s to the group name at slot slot for the created wall or its constituent facets. If the slot keyword is not specified, then the group name is assigned to the slot Default . The optional facet keyword specifies that the group is assigned to the facets and not to the wall."
        },
        {
          "name": "id",
          "syntax": "id <id>",
          "description": "Wall ID. If not specified, then the next available ID is used."
        },
        {
          "name": "name",
          "syntax": "name <s>",
          "description": "Wall name. If not specified, then each shape defines a wall name with the ID post pended."
        },
        {
          "name": "box",
          "syntax": "box <fxmin><fxmax><fymin><fymax><fzmin><fzmax>[one-wall] [expand <fe>] `z`-components are 3D ONLY",
          "description": "Generate a closed box. If the name keyword has not been specified, then s = boxWall . By default, each side of the box is a separate wall. In this case, the wall name is concatenated with the wall side name (e.g., {top, bottom, left, or right in 2d; top, bottom, left, right, front, or back in 3D}) and the wall ID. For instance, if s = fred , the name of the top wall is fredTopXX , where XX is the wall ID. A unique ID, starting with id , is assigned to each wall. Use the one-wall keyword to specify that all box sides belong to a single wall. If fxmin and fxmax are specified alone, then the box is a cube with equal sides. Use the optional expand keyword to expand each side of the box by a factor fe ."
        },
        {
          "name": "polygon",
          "syntax": "polygon <vpos>... [make-planar] [triangulate] 3D ONLY",
          "description": "Generate a planar, polygonal wall in 3D. If the name keyword has not been specified, then s = polygonWall . There are two modes of operation: the default mode which is guaranteed to work for convex polygons and the triangulation mode, specified with the optional triangulate keyword, that works for both convex and concave polygons. In both modes, the makeplanar keyword is used to snap the vertices to the plane with point equal to the average location of vertices (called the center). The plane normal is defined by the cross product of two vectors, namely the vector difference between the center and vertex 1 and the vector difference between the center and vertex 2. In the default mode, the vertices of the wall can be input in any order and the command is guaranteed to work for sets of vertices defining convex polygons. The algorithm proceeds by ordering the points in clockwise order about the center. The first facet is created from the [1,2,num] vertex where num is the total number of vertices. The next facet is made from the [2,num,3] vertices, and the following from the [num,3,num-1] vertices, and so on. In triangulation mode, the vertices must be input in an ordered fashion around the perimeter of the polygon. The sweep line algorithm is used to triangulate the polygon."
        }
      ],
      "examples": [
        {
          "command": "wall generate box -10 10",
          "description": "Generate cube with sides at ±10 (separate walls for each face)"
        },
        {
          "command": "wall generate box -5 5 -3 3 -2 2 one-wall",
          "description": "Generate rectangular box as single wall (3D)"
        },
        {
          "command": "wall generate sphere position (0,0,5) radius 2.5",
          "description": "Generate sphere at (0,0,5) with radius 2.5 (3D)"
        },
        {
          "command": "wall generate cylinder base (0,0,0) axis (0,0,1) height 10 radius 3 cap false true",
          "description": "Generate cylinder with no bottom cap, top cap only (3D)"
        },
        {
          "command": "wall generate disk radius 5.0 dip 45.0 dip-direction 90.0",
          "description": "Generate disk with 45° dip, 90° dip direction (3D)"
        },
        {
          "command": "wall generate cone base (-2.5,0,-5) axis (1,1,1) height 2.0 radius 0.1 2.5 cap true true resolution 0.5 one-wall",
          "description": "Generate conic frustum as single wall with both caps (3D)"
        },
        {
          "command": "wall generate circle position (0,0) radius 3.0 resolution 0.05",
          "description": "Generate circle at origin with radius 3.0, fine resolution (2D)"
        },
        {
          "command": "wall generate plane position (0,0,10) dip 30 dip-direction 45",
          "description": "Generate plane through (0,0,10) with 30° dip at 45° direction (3D)"
        },
        {
          "command": "wall generate name 'boundary' group 'walls' box -10 10 -10 10 0 20",
          "description": "Generate named box in group 'walls' (3D)"
        }
      ],
      "figure_reference": {
        "figures": [
          {
            "name": "cones.png",
            "doc_path": "_images/cones.png"
          },
          {
            "name": "spheres.png",
            "doc_path": "_images/spheres.png"
          }
        ],
        "note": "This command embeds reference figures that are not captured in this text corpus. Figures are PNGs under <doc-root>/_images/ (doc-root sits beside the engine install, e.g. C:/Program Files/Itasca/<product>/exe64/doc); read them directly."
      },
      "notes": [
        "resolution/position are not live on 9.7; the duplicate keyword entries were crawler artifacts."
      ]
    }
  }
}
