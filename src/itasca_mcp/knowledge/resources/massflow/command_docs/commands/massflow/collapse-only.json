{
  "category": "massflow",
  "search_keywords": [
    "massflow",
    "collapse-only",
    "collapse"
  ],
  "description": "Listed by `massflow ?` on the 9.7 binary. No manual page exists and its keyword set is not known: enumerating it with `massflow collapse-only ?` terminates the MassFlow process (reproduced twice on 9.7.47), so the keyword slot has never been read.",
  "python_sdk_alternative": {
    "available": false
  },
  "versions": {
    "9.0": {
      "command": "massflow collapse-only",
      "syntax": "massflow collapse-only",
      "keywords": [],
      "examples": []
    }
  },
  "notes": [
    "Not in the MassFlow manual — this command has no page in the 9.7 doc tree. Syntax below is read from the live 9.7.47 binary.",
    "Do not probe this command with `?` — it kills the engine process."
  ]
}
