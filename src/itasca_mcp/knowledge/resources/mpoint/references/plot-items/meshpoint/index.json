{
  "name": "meshpoint",
  "item_type": "meshpoint",
  "item_types": [
    "meshpoint"
  ],
  "dimension": "mixed",
  "search_keywords": [
    "mesh point",
    "background grid",
    "node",
    "grid"
  ],
  "description": "Background-grid mesh points — the fixed computational grid through which material points move. Use to inspect the grid resolution and extent relative to the material-point body.",
  "base_syntax": "plot item create meshpoint <keywords...>",
  "top_level_keywords": [
    "active",
    "clear",
    "clip",
    "cut",
    "global",
    "label",
    "legend",
    "map",
    "pixel-size",
    "quality",
    "range",
    "transparency"
  ],
  "basic_keywords": [
    {
      "keyword": "active",
      "description": "Show or hide the item without removing it.",
      "syntax": "active <bool>"
    },
    {
      "keyword": "label",
      "description": "Bare switch that turns on categorical colouring of grid nodes and opens a sub-context: 'label extra <1..128>', 'label fixity', 'label group <string>', 'label uniform'. Unlike mpoint-hybrid's 'label', it takes no string of its own -- 'label \"name\"' is rejected.",
      "syntax": "label [extra <int> | fixity | group <string> | uniform | ...]"
    },
    {
      "keyword": "global",
      "description": "Draw across the whole model (ignore per-plot clipping).",
      "syntax": "global <bool>"
    },
    {
      "keyword": "range",
      "description": "Filter which entities are drawn using range elements (see references/range-elements).",
      "syntax": "range <range-element> [...]"
    },
    {
      "keyword": "legend",
      "description": "Show/configure the item legend.",
      "syntax": "legend <sub-keyword> [<value>]"
    },
    {
      "keyword": "map",
      "description": "Configure the contour colour map (ranges, intervals, colours).",
      "syntax": "map <sub-keyword> [<value>]"
    },
    {
      "keyword": "cut",
      "description": "Apply a 3D cutting plane to the item.",
      "syntax": "cut active on origin (x,y,z) normal (nx,ny,nz)"
    },
    {
      "keyword": "transparency",
      "description": "Set item transparency (0 opaque .. 100 fully transparent).",
      "syntax": "transparency <0-100>"
    }
  ],
  "sub_items": [
    {
      "name": "label",
      "syntax": "plot item create meshpoint label [<sub-keyword> ...]",
      "description": "Categorical colouring of grid nodes. 'label' itself takes no argument; it opens a sub-context whose keywords are NOT offered at the item's top level.",
      "keywords": [
        "extra",
        "fixity",
        "group",
        "uniform"
      ],
      "verified": "state -- MPoint3D 9.7; each sub-keyword accepted after 'label', rejected before it"
    }
  ],
  "common_usage_patterns": [
    {
      "use_case": "Show background grid",
      "command": "plot item create meshpoint active on",
      "description": "Render the background-grid mesh points."
    }
  ],
  "notes": [
    "Probe live with 'plot item create <type> ?' to list top-level keywords, then 'plot item create <type> <kw> ?' for sub-options. Do NOT probe with a bogus token: 'plot item create <type> zzbogus' creates the item and then reports 'Unused extra parameter' without ever enumerating. Keyword sets here are binary-validated against MPoint 3D 9."
  ]
}
